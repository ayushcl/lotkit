# Production runtime dependencies. Keep versions aligned with requirements.txt;
# pytest is intentionally excluded because it is needed only by development/CI.
fastapi>=0.115,<0.129
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
httpx==0.28.1
Pillow==12.3.0
reportlab==5.0.0
pypdf==6.10.0
python-dotenv==1.2.2
